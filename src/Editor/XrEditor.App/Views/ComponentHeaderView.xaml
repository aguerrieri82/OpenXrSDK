<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:local="clr-namespace:XrEditor;assembly=XrEditor.Core"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate DataType="{x:Type local:ComponentHeaderView}">
        <StackPanel Orientation="Horizontal">
            <CheckBox VerticalAlignment="Center" IsChecked="{Binding IsEnabled, Mode=TwoWay}"/>
            <TextBox VerticalAlignment="Center" Text="{Binding Name}"/>
        </StackPanel>

    </DataTemplate>

</ResourceDictionary>